pipeline {
    agent { docker { image 'mcr.microsoft.com/dotnet/framework/samples:dotnetapp' } }
    stages {
        stage('build') {
            steps {
                bat 'set'
            }
        }
    }
}